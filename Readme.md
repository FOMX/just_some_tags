```markdown
# just_some_tags

Tiny C example that builds on Linux with make.

Build:
```sh
make
./bin/hello
```

Versions:
- v1.0.0: basic "Hello, world!"
- v1.1.0: prints explicit version string
- v2.0.0: adds install target in Makefile
```